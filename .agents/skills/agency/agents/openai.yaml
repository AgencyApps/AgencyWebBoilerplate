interface:
  display_name: "Agency"
  short_description: "Develop and operate Agency-backed apps"
  default_prompt: "Use $agency plus the Agency plugin tools to inspect the company, sync local env when needed, implement the product change, and ship both remotes when delivery is implied."

policy:
  allow_implicit_invocation: true
