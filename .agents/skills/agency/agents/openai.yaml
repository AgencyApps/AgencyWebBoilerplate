interface:
  display_name: "Agency"
  short_description: "Build and ship Agency company changes"
  default_prompt: "Use $agency to implement this Agency company change with the bundled SDKs, verify it, and ship the repo update when delivery is implied."

policy:
  allow_implicit_invocation: true
